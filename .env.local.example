# Copy to .env.local (never commit .env.local)
# Used by scripts/import-catalog.mjs

RADIO_SERVICE_KEY=your-long-random-secret-from-vercel
SITE_URL=https://www.the615hideaway.com

# Optional — after `npx vercel login` + `npx vercel link`:
# VERCEL_TOKEN=from vercel.com account tokens